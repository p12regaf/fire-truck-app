Esta es la clave de despliegue para el repositorio de GitHub (fire-truck-app):

Se debe sustituir este archivo por la clave privada correspondiente al par de claves SSH generado específicamente para el despliegue automático desde GitHub Actions al servidor donde se aloja la aplicación, renombrandolo con el nombre "deploy_key"